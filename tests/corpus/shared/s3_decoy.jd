/**
 * expect: NO FINDING. This is the decoy.
 * The leaf shares most of its words with s1_fairness.jd's "The committed training split"
 * but denotes an entirely different artifact -- config/train.yaml, not data/train.csv.
 * A reviewer clustering on string similarity will wrongly propose merging this with s1;
 * a reviewer clustering on the artifact named will not.
 */
justification reproducibility {
    conclusion c is "The training run is reproducible from committed inputs"
    strategy   s is "Confront the recorded run parameters with the committed training configuration; every parameter is pinned"
    evidence   e_cfg is "The committed training configuration"

    s supports c
    e_cfg supports s
}
