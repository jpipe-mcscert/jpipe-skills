/**
 * expect: JD-S01 (with s2_provenance.jd)
 * Grounds on data/train.csv. Pairs with s2_provenance.jd, which denotes the same artifact
 * under a drifted label -- so the two leaves will NOT unify under assemble, and the same
 * fact is stated twice.
 */
justification fairness {
    conclusion c is "No protected attribute reaches the model"
    strategy   s is "Confront the training split's column set with the protected-attribute list in SPECS section 4; the intersection is empty"
    evidence   e_train is "The committed training split"

    s supports c
    e_train supports s
}
