/**
 * expect: JD-M01 (with s1_fairness.jd)
 * Grounds on the same artifact as s1_fairness.jd -- data/train.csv -- but words the leaf
 * differently. The warrant is genuinely different, and that is fine: sharing a fact does
 * not mean sharing the check. Only the leaves should be aligned.
 */
justification provenance {
    conclusion c is "The training split is the frozen committed one"
    strategy   s is "Confront the split's checksum with the one recorded at freeze time; they match"
    evidence   e_data is "The train.csv split as committed"

    s supports c
    e_data supports s
}
